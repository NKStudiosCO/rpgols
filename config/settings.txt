# fullscreen mode. 1 enable, 0 disable.
fullscreen=1

# display resolution. 640x480 minimum.
resolution_w=1680
resolution_h=1050

# Enable music and sound subsystem.
audio=1

# music and sound volume (0 = silent, 128 = max)
music_volume=96
sound_volume=128

# display floating damage text. 1 enable, 0 disable.
combat_text=0

# use mouse to move (experimental). 1 enable, 0 disable.
mouse_move=0

# hardware surfaces, double buffering. Try disabling for performance. 1 enable, 0 disable.
hwsurface=1
doublebuf=1

# animated tiles. Try disabling for performance. 1 enable, 0 disable.
animated_tiles=1

# joystick settings.
enable_joystick=0
joystick_device=0
joystick_deadzone=100

# 2-letter language code.
language=en

# allow changing gamma (experimental). 1 enable, 0 disable.
change_gamma=0

# screen gamma (0.5 = darkest, 2.0 = lightest)
gamma=1

# texture quality (0 = low quality, 1 = high quality)
texture_quality=1

# use mouse to aim. 1 enable, 0 disable.
mouse_aim=1

# make using mouse secondary, give full control to keyboard. 1 enable, 0 disable.
no_mouse=0

# show frames per second. 1 enable, 0 disable.
show_fps=0

# show hotkeys names on power bar. 1 enable, 0 disable.
show_hotkeys=1

# enable colorblind tooltips. 1 enable, 0 disable
colorblind=0
